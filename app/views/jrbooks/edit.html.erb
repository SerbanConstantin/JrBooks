<div class="card">
  <div class="card-header">
  <h4>Editing Book</h4>
  </div>
  <div class="card-body">

<%= render 'form', jrbook: @jrbook %>
<br/>
</div>
</div>
<br/>
<%= link_to 'Show', @jrbook, class:"btn btn-primary" %> 

<%= link_to 'Back', jrbooks_path, class:"btn btn-primary" %>
<br/>
<br/>
<%= button_to 'Delete', @jrbook, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-danger btn-sm"%>
